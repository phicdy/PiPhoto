<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".PhotoListActivity">

    <item
        android:id="@+id/camera"
        android:icon="@mipmap/ic_camera_alt_white"
        app:showAsAction="ifRoom"
        android:title="@string/taking_photo"/>

    <item
        android:id="@+id/record"
        android:icon="@mipmap/ic_adjust_white"
        app:showAsAction="ifRoom"
        android:title="@string/taking_photo"/>

    <item
        android:id="@+id/download"
        android:icon="@mipmap/ic_file_download_white"
        app:showAsAction="ifRoom"
        android:title="@string/taking_photo"/>

</menu>
